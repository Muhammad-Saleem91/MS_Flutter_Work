void main()
{
  var count = 0; // it does not require to define data type
  var name = "Saleem"; // it will automatically recognize its data type
  String ms = " M";
 
  print(2 + 1);
  for(var i = 0;i < 10; i++ )  
  {
    //count = count + i;
    print(count + i); //function that displays method
    print(name + ms);
    
  } 
}
